const DefaultCard = ({children}: any) => {
    return (
        <>
            <div className="card default-card">
                {children}
            </div>
        </>
    );
}

export default DefaultCard